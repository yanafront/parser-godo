# 🚂 Настройка и запуск на Railway

## 📋 Как проверить что парсер работает

### 1. Локально (SQLite)

```bash
npm run local
```

**Признаки успешной работы:**
- ✅ В консоли: "Авторизация прошла успешно!"
- ✅ В консоли: "✅ Все обработчики событий добавлены. Бот работает!"
- ✅ HTTP сервер запущен: `http://localhost:3000`
- ✅ Проверка статуса: `curl http://localhost:3000/health`

**Эндпоинты для проверки:**
- `http://localhost:3000/health` - статус бота и количество сообщений
- `http://localhost:3000/messages` - все сохраненные сообщения

**Что смотреть в логах:**
```
📡 Подключаюсь к чату: @chat_name
📨 Получено сообщение из чата...
🤖 Отправляю в AI для обработки...
💾 Сохраняю в БД...
✅ Сообщение обработано и отправлено в @go_do_minsk
```

### 2. На Railway (PostgreSQL)

**Эндпоинты:**
- `https://ваш-проект.railway.app/health` - статус бота
- `https://ваш-проект.railway.app/messages` - сообщения из БД

**Ответ /health должен содержать:**
```json
{
  "status": "ok",
  "connected": true,
  "messageCount": 10,
  "uptime": 3600,
  "timestamp": "2024-01-01T12:00:00.000Z"
}
```

**Если `connected: false`** - бот не подключен к Telegram
**Если `messageCount` растет** - парсер работает и сохраняет сообщения

---

## 🚀 Деплой на Railway с PostgreSQL

### Шаг 1: Подготовка проекта

1. Закоммитьте изменения:
```bash
git add .
git commit -m "Ready for Railway deployment"
git push
```

### Шаг 2: Создание проекта на Railway

1. Зайдите на [railway.app](https://railway.app)
2. Создайте новый проект
3. Выберите "Deploy from GitHub repo" или "Empty Project"

### Шаг 3: Добавление PostgreSQL

1. В проекте нажмите "+ New"
2. Выберите "Database" → "PostgreSQL"
3. Railway автоматически создаст PostgreSQL сервис
4. Переменные окружения будут автоматически добавлены:
   - `PGHOST`
   - `PGUSER`
   - `PGPASSWORD`
   - `PGDATABASE`
   - `PGPORT`

### Шаг 4: Настройка переменных окружения

В настройках вашего сервиса добавьте:

**Telegram API:**
```
API_ID=ваш_api_id
API_HASH=ваш_api_hash
TG_PHONE=+375XXXXXXXXX
TG_SESSION=
TG_CODE=
TG_PASSWORD=
```

**OpenAI:**
```
OPENAI_API_KEY=sk-ваш_ключ
```

**Окружение:**
```
NODE_ENV=production
PORT=3000
```

### 🔑 Настройка авторизации Telegram

**Способ 1: Использование готовой сессии (рекомендуется)**

1. Запустите локально: `npm run local`
2. После успешной авторизации скопируйте сессию из консоли
3. Добавьте на Railway: `TG_SESSION=скопированная_сессия`
4. Оставьте `TG_CODE` пустым

**Способ 2: Первая авторизация на Railway**

Если у вас нет сессии:

1. Запустите деплой на Railway с пустыми `TG_SESSION` и `TG_CODE`
2. Получите код подтверждения из Telegram (придет в Telegram)
3. В Railway откройте логи и найдите ошибку с инструкцией
4. Добавьте переменную: `TG_CODE=код_из_телеграма` (например `12345`)
5. Railway автоматически перезапустится
6. После успешной авторизации в логах появится новая сессия:
   ```
   🔑 Текущая сессия:
   1BVtsOMwAy5... (длинная строка)
   ⚠️  Сессия изменилась! Обновите TG_SESSION на Railway:
   TG_SESSION=1BVtsOMwAy5...
   ```
7. Скопируйте сессию и добавьте: `TG_SESSION=скопированная_сессия`
8. **Удалите `TG_CODE`** (он больше не нужен)
9. Railway перезапустится и будет использовать сессию

### Шаг 5: Подключение к GitHub

1. В Railway проекте нажмите "Settings"
2. Выберите "Connect GitHub"
3. Выберите ваш репозиторий
4. Railway автоматически определит `Dockerfile` или `package.json`

### Шаг 6: Деплой

Railway автоматически:
1. Определит Dockerfile
2. Соберет проект (`npm run build`)
3. Запустит (`npm start`)

### Шаг 7: Проверка

1. Откройте логи в Railway
2. Убедитесь что видите:
   - "✅ Все переменные окружения загружены"
   - "✅ Таблица messages создана/проверена в PostgreSQL"
   - "Авторизация прошла успешно!"
   - "✅ Все обработчики событий добавлены. Бот работает!"

3. Откройте URL проекта: `https://ваш-проект.railway.app/health`

---

## 🔧 Альтернатива: Использование DATABASE_URL

Railway может предоставить `DATABASE_URL` вместо отдельных переменных.
Код автоматически поддерживает оба варианта:

```env
# Вариант 1: Отдельные переменные (Railway создает автоматически)
PGHOST=...
PGUSER=...
PGPASSWORD=...
PGDATABASE=...
PGPORT=...

# Вариант 2: Connection string (если Railway предоставляет)
DATABASE_URL=postgresql://user:password@host:port/database
```

---

## 🐛 Отладка проблем

### Бот не подключается к Telegram
- Проверьте `API_ID` и `API_HASH`
- Проверьте `TG_SESSION` (может быть устаревшей)
- Посмотрите логи на ошибки авторизации

### Ошибка подключения к БД
- Убедитесь что PostgreSQL сервис запущен
- Проверьте переменные окружения PostgreSQL
- Проверьте что сервис приложения подключен к сервису БД в Railway

### Сообщения не сохраняются
- Проверьте логи на ошибки обработки
- Проверьте `/health` - должен быть `connected: true`
- Проверьте `/messages` - должны быть сохраненные сообщения

### Парсер не получает сообщения
- Убедитесь что бот добавлен в нужные чаты
- Проверьте названия чатов в `index.ts` (строка 54)
- Проверьте права бота в чатах

